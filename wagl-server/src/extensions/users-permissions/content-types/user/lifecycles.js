module.exports = {
    async afterCreate(event) { 

             const { result, params } = event;

             

    },

    async afterDelete(event) {
        const { result, params } = event;
const  {id} = result;
      
       
      
    },
   
}